# CLAUDE.md

## Testing

- When adding or modifying a feature to the PowerPortalsPro.Dataverse.Linq query provider, always create both FetchXml generation unit tests (in the `FetchXml/` folder) and integration tests (in the `Integration/` folder).
